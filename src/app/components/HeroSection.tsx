import styles from "./HeroSection.module.css";

const HeroSection = () => {
  return (
    <section className={styles.hero}>
      <h1>Hero Section</h1>
    </section>
  );
};

export default HeroSection;
